apiVersion: v1
kind: Service
metadata:
  name: warp
  namespace: lae-development
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: /user/actuator/prometheus
spec:
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: warp